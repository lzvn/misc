print "What's your age? "
age = gets.to_i

puts age<18 ? "Go to school, kid" : "Get a job"

# it works just like c ternaries
# remeber to put a space between points, otherwise the second string becomes a symbol